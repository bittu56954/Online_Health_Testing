# MEDISCAN Environment Variable Configuration Template

# Backend Settings
PORT=5001
MONGO_URI=mongodb://127.0.0.1:27017/mediscan_db
JWT_SECRET=mediscan_super_secret_jwt_key_2026_safe_health_app
JWT_EXPIRE=30d

# Email Nodemailer (Optional for local testing, fallback to console log if blank)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM="MEDISCAN Health Security <noreply@mediscan.com>"

# Frontend Settings
CLIENT_URL=http://localhost:5173
VITE_API_URL=http://localhost:5001/api
