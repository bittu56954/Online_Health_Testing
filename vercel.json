{
  "version": 2,
  "buildCommand": "npm install && cd React && npm install && npx vite build",
  "outputDirectory": "React/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
